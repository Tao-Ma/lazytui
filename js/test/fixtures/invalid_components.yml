project_dir: .

groups:
  mygroup:
    label: My Group
    containers: []
    actions:
      hello:
        cmd: echo hello
        label: Hello

# Invalid: `components:` must be a LIST of module paths, not a scalar.
components: ./not-a-list.js
