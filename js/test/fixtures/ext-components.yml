project_dir: .

groups:
  mygroup:
    label: My Group
    containers: []
    actions:
      hello:
        cmd: echo hello
        label: Hello

components:
  - ./ext-hello.js      # `.`-relative → resolved to absolute against project_dir
  - some-bare-pkg       # bare name → kept for node require resolution
