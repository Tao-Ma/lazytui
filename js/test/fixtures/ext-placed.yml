project_dir: .

components:
  - ./ext-hello.js

panels:
  exthello: { type: ext-hello, title: ExtPane }
  groups:   { type: groups,    title: Groups }
  actions:  { type: actions,   title: Actions }
  detail:   { type: detail,    title: Detail }

groups:
  g1:
    label: G1
    containers: []
    actions:
      hello:
        cmd: echo hello
        label: Hello

layout:
  columns:
    - width: 30
      panels: [exthello, groups]
    - panels: [actions, detail]
